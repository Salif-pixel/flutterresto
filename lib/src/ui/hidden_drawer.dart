import 'package:flutter/material.dart';
import 'package:get/get.dart';

class Hidden_drawer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
